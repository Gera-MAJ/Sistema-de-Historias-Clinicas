import { createContext } from "react";
//Esto se hace para poder usarlo al contexto en todos lados y no tener que estar haciendo todo el tiempo este tipo de importacion
export const ProveedorDeContexto = createContext(null)

